{
  "name": "@app/api",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "src/server.js",
  "scripts": {
    "dev": "node --watch src/server.js",
    "build": "node -e \"console.log('No build step for JavaScript API')\"",
    "start": "node src/server.js",
    "lint": "eslint .",
    "typecheck": "node -e \"console.log('No typecheck step for JavaScript API')\"",
    "test": "vitest run --passWithNoTests",
    "clean": "rimraf dist .turbo"
  },
  "dependencies": {
    "@repo/errors": "workspace:*",
    "@repo/logger": "workspace:*",
    "@repo/validation": "workspace:*",
    "cors": "^2.8.5",
    "dotenv": "^16.4.7",
    "express": "^4.21.2"
  },
  "devDependencies": {
    "@repo/eslint-config": "workspace:*",
    "eslint": "^9.17.0",
    "rimraf": "^6.0.1",
    "vitest": "^2.1.8"
  }
}
